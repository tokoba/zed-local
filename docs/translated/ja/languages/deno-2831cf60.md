# Deno

Deno のサポートは [Deno extension](https://github.com/zed-extensions/deno) を通じて利用できます。

- 言語サーバー: [Deno Language Server](https://docs.deno.com/runtime/manual/advanced/language_server/overview/)

## Deno の設定

TypeScript と TSX ファイルで Deno Language Server を使用するには、デフォルトの言語サーバーを無効にして Deno を有効にするとよいでしょう。

言語サーバーとフォーマッタは、Settings ({#kb zed::OpenSettings}) の Languages > JavaScript/TypeScript/TSX で設定するか、設定ファイルに次を追加します:

```json [settings]
{
  "lsp": {
    "deno": {
      "settings": {
        "deno": {
          "enable": true
        }
      }
    }
  },
  "languages": {
    "JavaScript": {
      "language_servers": [
        "deno",
        "!typescript-language-server",
        "!vtsls",
        "!eslint"
      ],
      "formatter": "language_server"
    },
    "TypeScript": {
      "language_servers": [
        "deno",
        "!typescript-language-server",
        "!vtsls",
        "!eslint"
      ],
      "formatter": "language_server"
    },
    "TSX": {
      "language_servers": [
        "deno",
        "!typescript-language-server",
        "!vtsls",
        "!eslint"
      ],
      "formatter": "language_server"
    }
  }
}
```

詳しくは、Zed ドキュメントの [Configuring supported languages](../configuring-languages.md) を参照してください。

<!--
TBD: Deno TypeScript REPL の手順 [docs/repl#typescript-deno](../repl.md#typescript-deno)
-->

## 設定の補完

`deno.json` や `package.json` の補完を有効にするには、設定ファイルに次を追加します（[編集方法](../configuring-zed.md#settings-files)）。詳細については [JSON](./json.md) を参照してください。

```json [settings]
"lsp": {
    "json-language-server": {
      "settings": {
        "json": {
          "schemas": [
            {
              "fileMatch": [
                "deno.json",
                "deno.jsonc"
              ],
              "url": "https://raw.githubusercontent.com/denoland/deno/refs/heads/main/cli/schemas/config-file.v1.json"
            },
            {
              "fileMatch": [
                "package.json"
              ],
              "url": "https://www.schemastore.org/package"
            }
          ]
        }
      }
    }
  }
```

## DAP サポート

Deno プログラムをデバッグするには、これを `.zed/debug.json` に追加します。

```json [debug]
[
  {
    "adapter": "JavaScript",
    "label": "Deno",
    "request": "launch",
    "type": "pwa-node",
    "cwd": "$ZED_WORKTREE_ROOT",
    "program": "$ZED_FILE",
    "runtimeExecutable": "deno",
    "runtimeArgs": ["run", "--allow-all", "--inspect-wait"],
    "attachSimplePort": 9229
  }
]
```

## Runnable サポート

テストなどの deno タスクを UI から実行するには、これを `.zed/tasks.json` に追加します。

```json [tasks]
[
  {
    "label": "deno test",
    "command": "deno test -A '$ZED_FILE'",
    "tags": ["js-test"]
  }
]
```

## 関連項目

- [TypeScript](./typescript.md)
- [JavaScript](./javascript.md)
